# Supabase Migration Archive

The remote Scampagnate EU project had a migration history that did not match
the local `supabase/migrations` folder. The old local migrations are preserved
under `pre_baseline_20260516105707/` for audit/reference, while active future
migrations should live in `supabase/migrations`.

Active baseline:

- `supabase/migrations/20260516105707_baseline_remote_schema.sql`

Remote project:

- `istotjnoqtrtthnyreyv` (`Scampagnate EU`)

The baseline was dumped from the live remote schema after the May 16, 2026
backend fixes were applied.
